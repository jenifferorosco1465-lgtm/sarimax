[project]
name = "sarimax-replica-ecuador"
version = "0.1.0"
description = "Reproducible SARIMAX replication for Ecuador inflation and ENSO"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
